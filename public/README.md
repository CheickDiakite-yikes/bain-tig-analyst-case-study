# Public Assets Folder

This folder is for static assets that need to be served directly at the root of your website. 

**Instructions for your Open Graph Image:**
1. Upload your image into this `public` folder.
2. Make sure the file is named exactly `og-image.png`.

Once you do that, your social sharing previews will automatically work!
